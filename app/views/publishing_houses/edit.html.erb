<% content_for :title, "Modifier la maison d’édition" %>

<div class="container py-5 mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1 class="fw-bold text-primary mb-4">🛠 Modifier <%= @publishing_house.name %></h1>

      <%= render 'form', publishing_house: @publishing_house %>

      <%= link_to "⬅️ Retour", publishing_house_path(@publishing_house), class: "btn btn-link mt-3" %>
    </div>
  </div>
</div>
