<%= form_with(model: chronicle, local: true, multipart: true) do |form| %>
  <% if chronicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chronicle.errors.count, "error") %> prohibited this chronicle from being saved:</h2>
      <ul>
        <% chronicle.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.file_field :photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
