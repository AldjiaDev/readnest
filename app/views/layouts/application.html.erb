<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Readnest" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap" rel="stylesheet">

    <!-- Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" crossorigin="" />
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <%= favicon_link_tag 'logo.jpg' %>
  </head>

  <body class="d-flex flex-column min-vh-100 bg-light">

    <!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top border-bottom py-2">
  <div class="container d-flex align-items-center justify-content-between">

   <!-- Logo -->
   <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "logo.jpg", alt: "Readnest", style: "height: 70px;" %>
    <% end %>



    <!-- Burger mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Liens principaux -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-3">
        <li class="nav-item">
          <%= link_to "Toutes les chroniques", chronicles_path, class: "nav-link custom-nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Maisons d’édition", publishing_houses_path, class: "nav-link custom-nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Librairies", bookshops_path, class: "nav-link custom-nav-link" %>
        </li>


        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Nouvelle chronique", new_chronicle_path, class: "nav-link custom-nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Mon profil", user_path(current_user), class: "nav-link custom-nav-link" %>
          </li>
        <% end %>
      </ul>

      <!-- Partie droite -->
      <div class="d-flex align-items-center gap-3">

        <% if user_signed_in? %>
          <!-- Notifications -->
          <%= link_to notifications_path, class: "position-relative text-dark" do %>
            <i class="bi bi-bell fs-5"></i>
            <% unread_count = current_user.received_notifications.unread.count %>
            <% if unread_count.positive? %>
              <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                <%= unread_count %>
              </span>
            <% end %>
          <% end %>

          <!-- Déconnexion -->
          <%= button_to "Se déconnecter", destroy_user_session_path,
                method: :delete,
                class: "btn btn-outline-dark rounded-pill px-3 fw-bold text-uppercase",
                form: { data: { turbo: false } } %>

        <% else %>
          <!-- Connexion / Inscription -->
          <%= link_to "Connexion", new_user_session_path, class: "btn btn-outline-dark rounded-pill px-3 fw-bold text-uppercase" %>
          <%= link_to "S’inscrire", new_user_registration_path, class: "btn btn-dark rounded-pill px-3 fw-bold text-uppercase" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

  <!-- Flash messages -->
  <div class="container" style="margin-top: 120px; max-width: 800px;">
    <% flash.each do |key, message| %>
      <div class="alert alert-<%= bootstrap_class_for(key) %> alert-dismissible fade show text-center rounded-3 shadow-sm" role="alert">
        <%= message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>
  </div>


    <!-- Contenu principal sans décalage -->
    <main class="flex-grow-1">
      <%= yield %>
    </main>

<!-- Footer -->
<footer class="bg-black text-white py-5">
  <div class="container">
    <div class="row text-center text-md-start justify-content-center align-items-start">

      <!-- Logo + Description -->
      <div class="col-md-5 mb-4 mb-md-0">
        <%= image_tag "logo.jpg", alt: "Readnest", style: "max-height: 70px;" %>
        <p class="small mt-3 mb-0">
          Une bibliothèque numérique où chaque chronique est une aventure littéraire.<br>
          Écrivez, découvrez, partagez.
        </p>
      </div>

      <!-- Navigation -->
      <div class="col-md-5">
        <h6 class="text-uppercase fw-bold mb-3">Navigation</h6>
        <ul class="list-unstyled small">
          <li class="mb-1"><%= link_to "Accueil", root_path, class: "text-white text-decoration-none" %></li>
          <li class="mb-1"><%= link_to "Toutes les chroniques", chronicles_path, class: "text-white text-decoration-none" %></li>
          <li class="mb-1"><%= link_to "Maisons d’édition", publishing_houses_path, class: "text-white text-decoration-none" %></li>
          <li class="mb-1"><%= link_to "Nouvelle chronique", new_chronicle_path, class: "text-white text-decoration-none" %></li>
          <li class="mb-1"><%= link_to "À propos", about_path, class: "text-white text-decoration-none" %></li>
        </ul>
      </div>

    </div>

    <hr class="border-light mt-4" />
    <p class="text-center small mb-0">&copy; <%= Time.current.year %> Readnest — Tous droits réservés.</p>
  </div>
</footer>



    <!-- JS -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" crossorigin=""></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
