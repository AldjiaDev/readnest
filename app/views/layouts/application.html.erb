<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Readnest" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (généré via asset pipeline) -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Leaflet CSS -->
    <link
      rel="stylesheet"
      href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-sA+e2XkG0tZDAvZSCaMtwU0I0C8n1NkPU9XdJr0rqtI="
      crossorigin=""
    />
  </head>

  <body class="d-flex flex-column min-vh-100 bg-light">

  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top border-bottom">
  <div class="container">
    <%= link_to "Readnest", root_path, class: "navbar-brand fw-bold text-brown" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item"><%= link_to "Mon profil", user_path(current_user), class: "nav-link text-brown" %></li>
          <li class="nav-item"><%= link_to "Nouvelle chronique", new_chronicle_path, class: "nav-link text-brown" %></li>
          <li class="nav-item">
            <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "nav-link text-danger", data: { turbo: false } %>
          </li>
        <% else %>
          <li class="nav-item"><%= link_to "Créer un compte", new_user_registration_path, class: "nav-link text-brown" %></li>
          <li class="nav-item"><%= link_to "Se connecter", new_user_session_path, class: "nav-link text-brown" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


    <!-- Page Content -->
    <main class="flex-grow-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="mt-5 bg-brown text-light py-4 shadow-sm">
  <div class="container">
    <div class="row text-center text-md-start">
      <div class="col-md-4 mb-3">
        <h5 class="text-uppercase fw-bold">Readnest</h5>
        <p class="small mb-0">Une bibliothèque numérique où chaque chronique est une aventure littéraire. Écrivez, découvrez, partagez.</p>
      </div>

      <div class="col-md-4 mb-3">
        <h6 class="fw-bold">Navigation</h6>
        <ul class="list-unstyled small">
          <li><%= link_to "Accueil", root_path, class: "text-light text-decoration-none" %></li>
          <li><%= link_to "Nouvelle chronique", new_chronicle_path, class: "text-light text-decoration-none" %></li>
          <li><%= link_to "Mon profil", user_signed_in? ? user_path(current_user) : "#", class: "text-light text-decoration-none" %></li>
        </ul>
      </div>

      <div class="col-md-4 mb-3">
        <h6 class="fw-bold">Suivez-nous</h6>
        <div class="d-flex gap-3 justify-content-center justify-content-md-start">
          <a href="#" class="text-light fs-5"><i class="bi bi-twitter-x"></i></a>
          <a href="#" class="text-light fs-5"><i class="bi bi-instagram"></i></a>
          <a href="#" class="text-light fs-5"><i class="bi bi-bookmark-star"></i></a>
        </div>
      </div>
    </div>

    <hr class="border-light" />
    <p class="text-center small mb-0">© <%= Time.current.year %> Readnest — Tous droits réservés.</p>
  </div>
</footer>


    <!-- JavaScript (Bootstrap + Rails) -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!-- Leaflet JS -->
    <script
      src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
      integrity="sha256-+9LU4tDyJMy4uqEVGZj+rfJHcb7Poyl3GzvA/j1C3yY="
      crossorigin=""
    ></script>
    <!-- Bootstrap JS via CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

  </body>
</html>
