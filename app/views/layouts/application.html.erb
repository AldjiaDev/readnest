<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Readnest" %></title>
    <%= display_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap" rel="stylesheet">

    <!-- Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" crossorigin="" />
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <%= favicon_link_tag 'logo.jpg' %>

    <title><%= page_title(title: content_for?(:title) ? yield(:title) : nil) %></title>
      <meta name="description" content="<%= content_for?(:meta_description) ? yield(:meta_description) : meta_description %>">

      <!-- OpenGraph -->
      <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : 'Readnest' %>">
      <meta property="og:description" content="<%= content_for?(:meta_description) ? yield(:meta_description) : meta_description %>">
      <meta property="og:type" content="website">
      <meta property="og:url" content="<%= request.original_url %>">
      <meta property="og:image" content="<%= image_url('logo.jpg') %>">

      <!-- Twitter -->
      <meta name="twitter:card" content="summary_large_image">
      <meta name="twitter:title" content="<%= content_for?(:title) ? yield(:title) : 'Readnest' %>">
      <meta name="twitter:description" content="<%= content_for?(:meta_description) ? yield(:meta_description) : meta_description %>">
      <meta name="twitter:image" content="<%= image_url('logo.jpg') %>">

      <%# Canonical URL pour SEO %>
    <%= tag(:link, rel: "canonical", href: request.original_url) if request.present? %>

      <title><%= content_for?(:title) ? yield(:title) : "Readnest" %></title>
      <%= display_meta_tags %>
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>
      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100 bg-light">

    <!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top border-bottom py-2">
  <div class="container d-flex align-items-center justify-content-between">

    <!-- Logo -->
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "logo.jpg", alt: "Readnest", style: "height: 70px;" %>
    <% end %>

    <!-- Burger mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Liens -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-3">

        <!-- Lien vers chroniques -->
        <li class="nav-item">
          <%= link_to "Chroniques", chronicles_path, class: "nav-link custom-nav-link" %>
        </li>

        <!-- Maisons d’édition -->
        <li class="nav-item">
          <%= link_to "Maisons d’édition", publishing_houses_path, class: "nav-link custom-nav-link" %>
        </li>

        <!-- Librairies -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle custom-nav-link" href="#" id="bookshopsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Librairies
          </a>
          <ul class="dropdown-menu" aria-labelledby="bookshopsDropdown">
            <li><%= link_to "Toutes les librairies", bookshops_path, class: "dropdown-item" %></li>
            <li><%= link_to "Autour de moi", bookshops_path(anchor: "map"), class: "dropdown-item" %></li>
          </ul>
        </li>
      </ul>

      <!-- Partie droite -->
      <div class="d-flex align-items-center gap-3">

        <% if user_signed_in? %>
          <!-- Notifications -->
          <%= link_to notifications_path, class: "position-relative text-dark me-3" do %>
            <i class="bi bi-bell fs-5"></i>
            <% unread_count = current_user.received_notifications.unread.count %>
            <% if unread_count.positive? %>
              <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                <%= unread_count %>
              </span>
            <% end %>
          <% end %>

          <!-- Dropdown utilisateur -->
          <div class="dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if current_user.avatar.attached? %>
                <%= image_tag current_user.avatar, class: "rounded-circle me-2", style: "width: 32px; height: 32px; object-fit: cover;" %>
              <% else %>
                <i class="bi bi-person-circle fs-4 me-1"></i>
              <% end %>
              <span><%= current_user.username %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><%= link_to "Mon profil", user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to "Nouvelle chronique", new_chronicle_path, class: "dropdown-item" %></li>
              <li>
                <%= link_to conversations_path, class: "dropdown-item position-relative" do %>
                  Messagerie
                  <% unread_count = Message.where(read: false).where.not(user: current_user).count %>
                  <% if unread_count.positive? %>
                    <span class="badge rounded-pill bg-danger ms-2"><%= unread_count %></span>
                  <% end %>
                <% end %>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= button_to "Se déconnecter", destroy_user_session_path,
                      method: :delete,
                      class: "dropdown-item text-danger",
                      form: { data: { turbo: false } } %>
              </li>
            </ul>
          </div>

        <% else %>
          <!-- Connexion / Inscription -->
          <%= link_to "Connexion", new_user_session_path, class: "btn btn-outline-dark rounded-pill px-3 fw-bold text-uppercase" %>
          <%= link_to "S’inscrire", new_user_registration_path, class: "btn btn-dark rounded-pill px-3 fw-bold text-uppercase" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>


  <!-- Flash messages -->
  <div class="container" style="margin-top: 120px; max-width: 800px;">
    <% flash.each do |key, message| %>
      <div class="alert alert-<%= bootstrap_class_for(key) %> alert-dismissible fade show text-center rounded-3 shadow-sm" role="alert">
        <%= message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>
  </div>


    <!-- Contenu principal sans décalage -->
    <main class="flex-grow-1">
      <%= yield %>
    </main>

<!-- Footer -->
<footer class="bg-black text-white py-5 mt-5">
  <div class="container">
    <div class="row align-items-center">

      <!-- Colonne gauche : Logo -->
      <div class="col-md-6 text-center text-md-start mb-4 mb-md-0">
        <%= image_tag "logo.jpg", alt: "Readnest", style: "max-height: 80px;" %>
        <p class="text-white-50 small mt-3 mb-0 fst-italic">
          « Une bibliothèque numérique où chaque chronique est une aventure littéraire. »
        </p>
      </div>

      <!-- Colonne droite : Navigation + Copyright -->
      <div class="col-md-6 text-center text-md-end">
        <!-- Navigation -->
        <nav class="mb-3">
          <ul class="list-inline mb-0">
            <li class="list-inline-item mx-2">
              <%= link_to "Accueil", root_path, class: "text-white text-decoration-none fw-semibold footer-link" %>
            </li>
            <li class="list-inline-item mx-2">
              <%= link_to "Chroniques", chronicles_path, class: "text-white text-decoration-none fw-semibold footer-link" %>
            </li>
            <li class="list-inline-item mx-2">
              <%= link_to "Maisons d’édition", publishing_houses_path, class: "text-white text-decoration-none fw-semibold footer-link" %>
            </li>
            <li class="list-inline-item mx-2">
              <%= link_to "Librairies", bookshops_path, class: "text-white text-decoration-none fw-semibold footer-link" %>
            </li>
            <li class="list-inline-item mx-2">
              <%= link_to "À propos", about_path, class: "text-white text-decoration-none fw-semibold footer-link" %>
            </li>
          </ul>
        </nav>

        <!-- Copyright -->
        <p class="small text-white-50 mb-0">
          &copy; <%= Time.current.year %> <strong>Readnest</strong> — Tous droits réservés.
        </p>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer-link:hover {
    color: #f8d210 !important; /* doré au hover */
    transition: color 0.3s ease-in-out;
  }
</style>


<style>
  .footer-link:hover {
    color: #f8d210 !important; /* jaune doré élégant au hover */
    transition: color 0.3s ease-in-out;
  }
</style>

    <!-- JS -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" crossorigin=""></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
