<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Readnest" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap" rel="stylesheet">

    <!-- Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" crossorigin="" />
  </head>

  <body class="d-flex flex-column min-vh-100 bg-light">

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top border-bottom">
      <div class="container">
        <%= link_to "📚 Readnest", root_path, class: "navbar-brand fw-bold text-brown" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto align-items-center">

            <!-- Liens visibles par tous -->
            <li class="nav-item">
              <%= link_to "Toutes les chroniques", chronicles_path, class: "nav-link text-brown" %>
            </li>
            <li class="nav-item">
              <%= link_to "Maisons d’édition", publishing_houses_path, class: "nav-link text-brown" %>
            </li>

            <% if user_signed_in? %>
              <!-- Notifications -->
              <li class="nav-item mx-2 position-relative">
                <%= link_to notifications_path, class: "nav-link position-relative px-2" do %>
                  🔔
                  <% unread_count = current_user.received_notifications.unread.count %>
                  <% if unread_count.positive? %>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                      <%= unread_count %>
                    </span>
                  <% end %>
                <% end %>
              </li>

              <!-- Profil utilisateur -->
              <li class="nav-item">
                <%= link_to "Mon profil", user_path(current_user), class: "nav-link text-brown" %>
              </li>

              <!-- Nouvelle chronique -->
              <li class="nav-item">
                <%= link_to "Nouvelle chronique", new_chronicle_path, class: "nav-link text-brown" %>
              </li>

              <!-- Déconnexion -->
              <li class="nav-item">
                <%= button_to "Se déconnecter", destroy_user_session_path,
                      method: :delete,
                      class: "btn btn-link nav-link text-danger",
                      form: { data: { turbo: false } } %>
              </li>
            <% else %>
              <!-- Non connecté -->
              <li class="nav-item">
                <%= link_to "S'inscrire", new_user_registration_path, class: "nav-link text-brown" %>
              </li>
              <li class="nav-item">
                <%= link_to "Connexion", new_user_session_path, class: "nav-link text-brown" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Main content (avec décalage navbar) -->
    <main class="flex-grow-1 pt-5 mt-4">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="mt-5 bg-brown text-light py-4 shadow-sm">
      <div class="container">
        <div class="row text-center text-md-start">
          <div class="col-md-4 mb-3">
            <h5 class="text-uppercase fw-bold">Readnest</h5>
            <p class="small mb-0">Une bibliothèque numérique où chaque chronique est une aventure littéraire. Écrivez, découvrez, partagez.</p>
          </div>

          <div class="col-md-4 mb-3">
            <h6 class="fw-bold">Navigation</h6>
            <ul class="list-unstyled small">
              <li><%= link_to "Accueil", root_path, class: "text-light text-decoration-none" %></li>
              <li><%= link_to "Toutes les chroniques", chronicles_path, class: "text-light text-decoration-none" %></li>
              <li><%= link_to "Maisons d’édition", publishing_houses_path, class: "text-light text-decoration-none" %></li>
              <li><%= link_to "Nouvelle chronique", new_chronicle_path, class: "text-light text-decoration-none" %></li>
            </ul>
          </div>

          <div class="col-md-4 mb-3">
            <h6 class="fw-bold">Suivez-nous</h6>
            <div class="d-flex gap-3 justify-content-center justify-content-md-start">
              <a href="#" class="text-light fs-5"><i class="bi bi-twitter-x"></i></a>
              <a href="#" class="text-light fs-5"><i class="bi bi-instagram"></i></a>
              <a href="#" class="text-light fs-5"><i class="bi bi-bookmark-star"></i></a>
            </div>
          </div>
        </div>

        <hr class="border-light" />
        <p class="text-center small mb-0">© <%= Time.current.year %> Readnest — Tous droits réservés.</p>
      </div>
    </footer>

    <!-- JS -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" crossorigin=""></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
