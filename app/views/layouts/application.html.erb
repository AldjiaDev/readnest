<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Readnest" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (généré via asset pipeline) -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Leaflet CSS -->
    <link
      rel="stylesheet"
      href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-sA+e2XkG0tZDAvZSCaMtwU0I0C8n1NkPU9XdJr0rqtI="
      crossorigin=""
    />
  </head>

  <body class="d-flex flex-column min-vh-100 bg-light">

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
      <div class="container">
        <%= link_to "📚 Readnest", root_path, class: "navbar-brand fw-bold" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to "My Profile", user_path(current_user), class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "New Chronicle", new_chronicle_path, class: "nav-link" %></li>
              <li class="nav-item">
                <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link text-danger", data: { turbo: false } %>
              </li>
            <% else %>
              <li class="nav-item"><%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "Log In", new_user_session_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Page Content -->
    <main class="flex-grow-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="text-center py-4 border-top mt-5 bg-white">
      <p class="mb-0">© <%= Time.current.year %> Readnest. Built with ❤️ for readers.</p>
    </footer>

    <!-- JavaScript (Bootstrap + Rails) -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!-- Leaflet JS -->
    <script
      src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
      integrity="sha256-+9LU4tDyJMy4uqEVGZj+rfJHcb7Poyl3GzvA/j1C3yY="
      crossorigin=""
    ></script>
  </body>
</html>
